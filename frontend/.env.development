# WHY localhost:8000: in local dev, FastAPI runs on :8000 and Vite on :5173.
# Setting VITE_API_URL here makes fetch() calls go to the right place.
# This file is safe to commit — it contains no secrets, only a localhost URL.
VITE_API_URL=http://localhost:8000
